import SwiftUI

extension Color {
    /// Kantýna brand accent — matches Android `MaterialTheme.colorScheme.primary`.
    static let brandAccent = Color(red: 234 / 255, green: 88 / 255, blue: 12 / 255) // #EA580C
}
